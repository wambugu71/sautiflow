/// The two text field container styles defined by Material 3.
enum M3ETextFieldVariant {
  /// Filled container with a bottom activation indicator.
  filled,

  /// Transparent container with a full outline.
  outlined,
}
