/// The visual variants of an `M3ELoadingIndicator`.
///
/// Mirrors `LoadingIndicatorM3EVariant` from the reference implementation.
enum M3ELoadingIndicatorVariant {
  /// A floating morphing shape drawn directly on the surface.
  defaultStyle,

  /// The morphing shape inside a filled, rounded container.
  contained,
}
